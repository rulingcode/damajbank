import 'package:flutter/material.dart';

class TransactionPage extends StatelessWidget {
  const TransactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('انتقال دارایی'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('صفحه تراکنش‌ها - این بخش بعداً تکمیل می‌شود'),
      ),
    );
  }
}